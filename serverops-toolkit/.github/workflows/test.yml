name: tests

on:
  push:
    branches: ["main", "serverops-toolkit-v0.1", "serverops-toolkit-v0.2"]
  pull_request:
  workflow_dispatch:

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.8", "3.10", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Compile
        run: python -m compileall -q serverops serverops.py serverops_gui.py tests
      - name: Unit tests
        run: python -m unittest discover -s tests -v
